import React from "react";
import { Heading, Text } from "spectacle";

export default class Closing extends React.Component {
  render() {
    return(
      <div>
        <Heading size={1} fit caps lineHeight={1} textColor="secondary">
          Now, let's code a bit
        </Heading>
      </div>
    )
  }
}
