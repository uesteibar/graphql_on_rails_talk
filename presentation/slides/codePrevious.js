import React from "react";
import { Heading, CodePane } from "spectacle";

export default class CodePrevious extends React.Component {
  render() {
    return(
      <div>
        <Heading size={1} fit caps lineHeight={1} margin="0 0 50px" textColor="secondary">
          Let's see some code
        </Heading>
      </div>
    )
  }
}
